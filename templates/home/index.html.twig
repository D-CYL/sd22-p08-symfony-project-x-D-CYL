{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

<div class="container">

    {% if app.user %}
    <h1>Hello {{ app.user.name }}</h1>
    {% else %}
    <h1>Hello Guest</h1>
    {% endif %}

    <div class="container-fluid light unselectable">
        <br>
        <h1 class="text-light">
            lorem ipsum
        </h1>
    </div>

</div>
{% endblock %}
