<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="https://softivuspro.com/html/casino/casino/assets/img/logo/logo.png">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="dropdown">
                    <button class="btn btn-warning dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                        Menu
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                        <li><a class="dropdown-item text-dark" href="{{ path('app_game') }}">Games</a></li>
                        <li><a class="dropdown-item text-dark" href="{{ path('app_category') }}">Categories</a></li>
                        {% if app.user %}
                            {% if "ROLE_ADMIN" in app.user.roles %}
                                <li><a class="dropdown-item text-dark" href="{{ path('category_create') }}">Add Category</a></li>
                                <li><a class="dropdown-item text-dark" href="{{ path('game_create') }}">Add Game</a></li>
                            {% endif %}
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
        <div class="navbar-nav">
            {% if app.user %}
                {% if "ROLE_USER" in app.user.roles %}
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                            <a class="btn btn-warning dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                {{ app.user.username }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li><a class="dropdown-item text-dark" href="{{ path('app_profile', {'user': app.user.id}) }}">Profile</a></li>
                                <li><a class="dropdown-item text-dark" href="{{ path('app_logout') }}">Log Out</a></li>
                            </ul>
                        </li>
                    </ul>
                {% endif %}
            {% else %}
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="btn btn-warning dropdown-toggle" href="#" id="authDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Account
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="authDropdown">
                            <li><a class="dropdown-item text-dark" href="{{ path('app_register') }}">Sign Up</a></li>
                            <li><a class="dropdown-item text-dark" href="{{ path('app_login') }}">Log In</a></li>
                        </ul>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>

<style>
    .dropdown-item:hover {
        background-color: unset !important;
        color: inherit !important;
    }
</style>
